package net.talaatharb.survey.entity;

/**
 * Enumeration of answer types, mirroring question types for response storage.
 */
public enum AnswerType {
    TEXT,
    SELECTION,
    NUMERIC,
    DATE,
    TIME
}
